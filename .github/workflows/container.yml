name: container run

on:
  workflow_dispatch:

defaults:
  run:
    working-directory: ./.github/actions/hello-tf

jobs:
  container-job:
    runs-on: ubuntu-latest

    container:
      image: hashicorp/terraform
      
      volumes:
        - $PWD:/infra
        
      workdir:
        - /infra
steps:
  - run: version
  - run: init
